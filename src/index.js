const Base = require("./parent").default
const Square = require("./square").default
const Circle = require("./circle").default
const Line = require("./line").default

module.exports = {
  Square,
  Circle,
  Line,
  Base
}